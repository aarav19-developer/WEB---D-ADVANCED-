// Find out the sum of all the element inside an array using for loop.

// let arr = [1,2,3,4,5,6]
// let sum = 0;

// for(let i = 0;)

// reduce method:
// let arr = [1,2,3,4,5]
// let res = arr.reduce((acc,current)=> console.log(acc,"acc",current,"current"))
// console.log(res)

//  map vs filter vs reduce:

// map: in this new array's length is same as original array
// filter: in this new array is the subset of the original array
// reduce: in this new array will only give single value
